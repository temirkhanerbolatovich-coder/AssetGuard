# AssetGuard MVP collection policy for GLPI Agent 1.19.
# This is a profile fragment: it does not define a server or any credentials.
# The collecting script supplies writable local/vardir paths at runtime.
no-category = accesslog,antivirus,battery,database,environment,firewall,input,licenseinfo,local_group,local_user,lvm,modem,port,printer,process,provider,psu,registry,remote_mgmt,rudder,slot,software,sound,usb,user,virtualmachine
